<!DOCTYPE html>
<html lang="en">
  <head>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <title>Cats, Cats, Cats</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="style.css" />
    <link rel="shortcut icon" href="images/favicon.ico" type="image/x-icon" />
  </head>
  <body>
    <div class="iphoneBackground">
    
      <div class="navContainer">
     
      <h1 class="navHeaders">SEECATS</h1>
      <h2 class="navHeaders">"TheShazam for Cats"</h2>
      <span class="startMenu" onclick="upload()">Let's Get Started</span>
      </div>

      <!-- Video access -->
     <video id="player" controls autoplay></video>
      <button id="capture">Capture</button>
      <canvas id="canvas" width=320 height=240></canvas>
      <script>
        //saving image
        const download = document.getElementById('canvas');
        download.addEventListener('click', function(e) {
        const link = document.createElement('a');
        link.download = 'download.png';
        link.href = canvas.toDataURL();
        link.click();
        link.delete;
      });


        const player = document.getElementById('player');
        const canvas = document.getElementById('canvas');
        const context = canvas.getContext('2d');
        const captureButton = document.getElementById('capture');
      
        const constraints = {
          video: true,
        };
      
        captureButton.addEventListener('click', () => {
          // Draw the video frame to the canvas.
          context.drawImage(player, 0, 0, canvas.width, canvas.height);
        });
      
        // Attach the video stream to the video element and autoplay.
        navigator.mediaDevices.getUserMedia(constraints)
          .then((stream) => {
            player.srcObject = stream;
          });
      </script> 
    <!-- _______________   -->

    <div class="buttonContainer"> 
        <h3 class="catH2">Touch to SEECATS</h3>
        <form action="/" enctype="multipart/form-data" method="post">
          <input type="file" name="file-to-upload" class="btn btn-info" capture="environment"  accept="image/*"/>
          <input type="submit" value="Upload" class="submit btn " />
        </form>
    </div>
  </div>
  </body>


  <script>
 function upload() {

let startMenu = document.querySelector(".startMenu")
let buttonContainer = document.querySelector(".buttonContainer")

buttonContainer.style.display = "flex"
startMenu.classList.add("startMenuHidden")
}
  </script>
</html>
